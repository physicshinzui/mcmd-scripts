        subroutine err_msg(idn)
c******************************************************
        include "COMDAT"
c******************************************************
        if(idn.eq.1) then
          print*,' ?????????????????????????????'
          print*,' ? nvert is too small. Stop. ?'
          print*,' ?????????????????????????????'
          stop
        endif

        if(idn.eq.2) then
          print*,' ???????????????????????'
          print*,' ? nvchg is too small. ?'
          print*,' ???????????????????????'
          stop
        endif

        if(idn.eq.3) then
          print*,' ??????????????????????????????'
          print*,' ? No vertial state assigned. ?'
          print*,' ??????????????????????????????'
          stop
        endif

        if(idn.eq.4) then
          print*,' ?????????????????????????????????????'
          print*,' ? Detected ikkone_use = 0. Strange. ?'
          print*,' ?????????????????????????????????????'
          stop
        endif
c******************************************************
        return
        end
